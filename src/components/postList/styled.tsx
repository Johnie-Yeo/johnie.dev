import styled from "styled-components"
import { rhythm } from "../../utils/typography"

const Title = styled.h1`
  margin-top: 35px;
`

const PostListBundle = styled.div`
  margin: ${rhythm(1)} 0;
`
export { Title, PostListBundle }
